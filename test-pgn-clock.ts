const { parsePgnToMoveTree, buildPgnFromMoveTree } = require('./apps/web/src/features/database/pgnUtils.ts'); // Wait, require won't work easily with TS.

// Let's just create a test in TS and run it with ts-node.
